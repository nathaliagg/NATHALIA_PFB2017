#!/usr/bin/env python3

import sys

num=int(sys.argv[1])

if num > 0 :
	print (" ")
	if (num>0 and num<=50):
		print (num, "is a positive number and smaller than 50")
	if (num>0 and num>=50):
                print (num, "is positive and greater than 50")
	if (num>0 and num<=50 and num%2==0):
        	print (num, "is a positive even number smaller than 50")
	if (num>0 and num<=50 and num%2==1):
		print (num, "is positive and divisible by 3 and greater than 50")
else :
	print (num, "is negative")


